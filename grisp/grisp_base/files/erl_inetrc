%--- Erlang Inet Configuration -------------------------------------------------

% Do not monitor the hosts file
{hosts_file, ""}.

{nameserver, {8,8,8,8}}.

% Disable caching
{cache_size, 0}.

%% Use Erlang DNS client
{edns, 0}.

% Specify lookup method
{lookup, [file, dns]}.
